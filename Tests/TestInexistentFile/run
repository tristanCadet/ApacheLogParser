FILE="$(cat /dev/urandom | tr -cd 'a-f0-9' | head -c 16)/$(cat /dev/urandom | tr -cd 'a-f0-9' | head -c 16)" && echo "Erreur lors de l'ouverture du fichier $FILE." > stderr.out && echo "Lecture du fichier $FILE..." > std.out && ../../bin/analog -g fichier.dot $FILE
